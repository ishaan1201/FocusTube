import React from 'react';

const VideoActions = () => {
  return (
    <div className="video-actions">
      {/* Save to vault, Share buttons */}
      <button>Save to Vault</button>
      <button>Share</button>
    </div>
  );
};

export default VideoActions;
