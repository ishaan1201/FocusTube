import React from 'react';

const CategorySelector = () => {
  return (
    <div className="category-selector">
      {/* For the user-selected categories later */}
      <button>Education</button>
      <button>Coding</button>
      <button>Music</button>
    </div>
  );
};

export default CategorySelector;
